@*@model IEnumerable<Green.Models.AdministrationModel>

@{
    ViewBag.Title = "TimeSlots";
}

    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Value)
            </th>
            <th></th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Value)
                </td>
                <td>
                    @*<h77 class="btn btn-primary">
                        @Html.ActionLink("Edit", "Edit", new { id = item.ID })
                        </h77>*@
                    @*<a href='@Url.Action("Edit",new { id=item.ID })' class="btn btn-xs btn-success"><span class="glyphicon glyphicon-edit"></span>Edit</a>
                </td>
            </tr>
        }
    </table>*@
@*<style type="text/css">
    .btn {
        background-color: white;
        color: black;
        border: 2px solid #4CAF50; /* Green */
    }
</style>*@